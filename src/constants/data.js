// All data is now fetched from API
export const heroSlides = [];
export const categories = [];
export const Products = [];

// Backwards compatibility exports (will be empty arrays but maintain structure)
export const bestsellers = {
  items: []
};

export const newArrivalsProducts = [];

// Instagram feed config (not API driven yet)
export const instagramFeed = {
  username: '@Alvira',
  description: 'Follow us on Instagram for styling inspiration and behind-the-scenes glimpses.'
};